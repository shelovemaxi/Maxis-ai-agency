# Nexus Agent Command Center

Responsive static dashboard for the Llama agent workspace.

## GitHub Pages

Enable Pages from **Settings → Pages**, select **Deploy from a branch**, branch `main`, folder `/ (root)`.
